import React from 'react'
import { useList } from '@pankod/refine-core'
import {
  PieChart,
  PropertyReferrals,
  TotalRevenue,
  PropertyCard,
  TopAgent
} from "components"

type Props = {}

const Home = (props: Props) => {
  return (
    <div>Home</div>
  )
}

export default Home